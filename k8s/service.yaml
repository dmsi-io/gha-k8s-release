apiVersion: v1
kind: Service
metadata:
  name: gha-k8s-release
  labels:
    app: gha-k8s-release
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: gha-k8s-release
    tier: web
